server:
  port: 8888

spring:
  application:
    name: config
  cloud:
    config:
      server:
        git:
          uri: https://github.com/shk95/msaJava17_Template
          default-label: main
          search-paths: Configs
